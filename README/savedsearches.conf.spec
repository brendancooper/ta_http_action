
[httpalert]
httpalert.param.endpoint = <string> Endpoint. It's a required parameter.
httpalert.param.qs_params = <string> Querystring Params. Use Comma , for separation and Key=Value.
httpalert.param.custom_headers = <string> Headers. Use Comma , for separation and Key=Value.
httpalert.param.payload = <string> Body. 
httpalert.param.method = <list> Method. It's a required parameter. It's default value is post.
httpalert.param.proxy = <string> Proxy to reach endpoint.
